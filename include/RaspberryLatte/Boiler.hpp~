#ifndef BOILER
#define BOILER

#include "PID.hpp"
#include "types.hpp"

namespace RaspLatte{
  class Boiler{
  private:
    Sensor<double> * temp_sensor_;
    PinIndex pwm_pin_;
    double set_point_;
    PID ctrl_;
    
  public:
    Boiler(Sensor<double> * temp_sensor, PinIndex pwm_pin, double set_point):
      temp_sensor_(temp_sensor), pwm_pin_(pwm_pin), set_point_(set_point), ctrl_({
      
    }
  };
#endif
